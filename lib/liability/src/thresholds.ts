/**
 * ثوابت المطابقة والتحقق المتقاطع — المصدر الموحّد.
 *
 * سابقًا كانت هذه القيم مكرّرة ومختلفة بين الخادم والتطبيق، ما أدّى إلى تطابق
 * حادثٍ في مسار دون آخر واختلاف درجات الثقة لنفس الزوج. توحيدها هنا يضمن أن
 * قرار المطابقة ونِسَب المسؤولية الناتجة عنه متطابقة بغضّ النظر عن المسار.
 */

/** ثوابت قرار المطابقة (evidence-based، النسخة الأدق). */
export const MATCH = {
  /** نصف نافذة البحث الزمني (± ملّي ثانية) — تلتقط انجراف الساعات. */
  TIME_WINDOW_MS: 60_000,
  /** أقصى فرق زمني مقبول قبل رفض المطابقة (ملّي ثانية). */
  MAX_TIME_DIFF_MS: 60_000,
  /** أقصى مسافة مقبولة عند توفّر GPS للطرفين (متر). */
  MAX_DISTANCE_M: 100,
  /** عتبة اعتبار زاويتَي الاقتراب "متقاربتَين" (درجات). */
  ANGLE_ALIGNED_DEG: 45,
  /** الحدّ الأدنى لدرجة الثقة لقبول المطابقة. */
  MIN_CONFIDENCE: 70,
  /** سقف درجة الثقة (لا يقين مطلق). */
  MAX_CONFIDENCE: 98,
} as const;

/** ثوابت التحقق المتقاطع (أعلام الاتّساق وأول تماس). */
export const CROSS = {
  /** أقصى مسافة GPS تُعدّ متّسقة (متر). */
  GPS_MAX_DISTANCE_M: 200,
  /** هامش تسامح فرق الوقت لأعلام الاتّساق (ملّي ثانية). */
  TIME_TOLERANCE_MS: 60_000,
  /** هامش انجراف ساعة الجهاز قبل الوثوق بترتيب "أول تماس" (ملّي ثانية). */
  CLOCK_DRIFT_MARGIN_MS: 5_000,
} as const;
